<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qst.project02main.mapper.AgeInfoMapper">

    <select id="loginList" parameterType="String" resultType="Integer">
        select num from logintime;
    </select>

    <select id="registList" parameterType="String" resultType="Integer">
        select num from registtime;
    </select>

    <select id="activeList" parameterType="String" resultType="Integer">
        select num from buyertime;
    </select>

</mapper>